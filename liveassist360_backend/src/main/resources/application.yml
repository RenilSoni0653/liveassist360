spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/live_assist_360
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  graphql:
    schema:
      printer:
        enabled: true
socket-server:
  port: 8085
  host: localhost
